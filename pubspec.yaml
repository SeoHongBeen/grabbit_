name: grabbit_project
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  # 공통
  shared_preferences: ^2.3.3
  intl: ^0.20.2
  http: ^1.2.2
  cupertino_icons: ^1.0.8
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  flutter_blue_plus: ^1.31.13
  timezone: ^0.9.2

  # Firebase
  firebase_core: ^4.0.0
  cloud_firestore: ^6.0.0
  firebase_auth: ^6.0.0    # 최신 안정권 (자동 호환)
  flutter_local_notifications: 17.2.3

  # 로컬 패스 말고, 호환되는 공식 패키지 사용

  # 푸시 꼭 필요할 때만 추가 (firebase_core 4.x 호환)
  # firebase_messaging: ^16.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/grabbit_logo.png
